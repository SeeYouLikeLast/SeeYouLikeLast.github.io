<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Github_page</title>
    <url>/2026/01/07/Github_page/</url>
    <content><![CDATA[<h2 id="Github-page"><a href="#Github-page" class="headerlink" title="Github_page"></a>Github_page</h2><h3 id="第一阶段：本地环境配置"><a href="#第一阶段：本地环境配置" class="headerlink" title="第一阶段：本地环境配置"></a>第一阶段：本地环境配置</h3><ol>
<li><p><strong>安装核心工具</strong>：安装 Node.js 和 Git。</p>
</li>
<li><p><strong>权限解锁</strong>：在 PowerShell 中执行 <code>Set-ExecutionPolicy RemoteSigned</code> 以允许运行 <code>npm</code> 脚本。</p>
</li>
<li><p><strong>安装 Hexo</strong>：运行 <code>npm install -g hexo-cli</code>。</p>
</li>
</ol>
<blockquote>
<p><strong>⚠️ 常见犯错提示：</strong></p>
<ul>
<li><p><strong>脚本禁用错误</strong>：如果你直接运行 <code>hexo</code> 报错“在此系统上禁止运行脚本”，记得一定要以<strong>管理员身份</strong>运行 PowerShell 并修改执行策略。</p>
</li>
<li><p><strong>版本不兼容</strong>：如果 <code>npm install</code> 极慢，建议使用国内镜像源（如 <code>npm config set registry https://registry.npmmirror.com</code>）。</p>
</li>
</ul>
</blockquote>
<hr>
<h3 id="第二阶段：GitHub-仓库准备"><a href="#第二阶段：GitHub-仓库准备" class="headerlink" title="第二阶段：GitHub 仓库准备"></a>第二阶段：GitHub 仓库准备</h3><ol>
<li><p><strong>创建特殊仓库</strong>：在 GitHub 上新建一个仓库。</p>
</li>
<li><p><strong>命名规范</strong>：仓库名必须为 <code>你的用户名.github.io</code>。</p>
</li>
</ol>
<blockquote>
<p><strong>⚠️ 常见犯错提示：</strong></p>
<ul>
<li><p><strong>命名失误</strong>：仓库名少写了 <code>.github.io</code> 或者用户名拼写错误，会导致无法通过域名直接访问博客。</p>
</li>
<li><p><strong>改名遗留问题</strong>：如果你修改过 GitHub 用户名，<strong>旧的仓库名必须同步手动重命名</strong>，否则 Hexo 无法找到推送目标。</p>
</li>
</ul>
</blockquote>
<hr>
<h3 id="第三阶段：本地初始化与部署配置"><a href="#第三阶段：本地初始化与部署配置" class="headerlink" title="第三阶段：本地初始化与部署配置"></a>第三阶段：本地初始化与部署配置</h3><ol>
<li><p><strong>初始化</strong>：在本地文件夹运行 <code>hexo init</code> 和 <code>npm install</code>。</p>
</li>
<li><p><strong>安装插件</strong>：运行 <code>npm install hexo-deployer-git --save</code>。</p>
</li>
<li><p><strong>配置 <code>_config.yml</code></strong>：修改文件底部的 <code>deploy</code> 部分：</p>
<p> YAML</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">deploy:</span><br><span class="line">  type: git</span><br><span class="line">  repo: https://github.com/用户名/用户名.github.io.git</span><br><span class="line">  branch: main</span><br></pre></td></tr></table></figure></li>
</ol>
<blockquote>
<p><strong>⚠️ 常见犯错提示：</strong></p>
<ul>
<li><p><strong>YAML 格式错误</strong>：<code>type:</code>, <code>repo:</code> 等冒号后面<strong>必须有一个空格</strong>，缩进必须对齐，否则会报 <code>Config Error</code>。</p>
</li>
<li><p><strong>分支名称错误</strong>：GitHub 现在默认分支名是 <code>main</code> 而不是 <code>master</code>，如果填错会导致部署成功但在网页上看不到内容。</p>
</li>
</ul>
</blockquote>
<hr>
<h3 id="第四阶段：笔记整理与上传流程"><a href="#第四阶段：笔记整理与上传流程" class="headerlink" title="第四阶段：笔记整理与上传流程"></a>第四阶段：笔记整理与上传流程</h3><p>这是你以后<strong>最常重复</strong>的步骤：</p>
<table>
<thead>
<tr>
<th><strong>步骤</strong></th>
<th><strong>命令</strong></th>
<th><strong>说明</strong></th>
</tr>
</thead>
<tbody><tr>
<td><strong>1. 新建</strong></td>
<td><code>hexo new &quot;标题&quot;</code></td>
<td>在 <code>source/_posts</code> 生成 .md 文件</td>
</tr>
<tr>
<td><strong>2. 编写</strong></td>
<td>使用 VS Code 编写</td>
<td>建议给 C++&#x2F;Java 笔记设置清晰的 <code>tags</code> 和 <code>categories</code></td>
</tr>
<tr>
<td><strong>3. 清理</strong></td>
<td><code>hexo clean</code></td>
<td><strong>必做</strong>，防止旧缓存导致页面显示异常</td>
</tr>
<tr>
<td><strong>4. 生成</strong></td>
<td><code>hexo g</code></td>
<td>将 Markdown 渲染成 HTML 网页</td>
</tr>
<tr>
<td><strong>5.本地预览</strong></td>
<td>hexo s</td>
<td>在浏览器打开 <code>http://localhost:4000</code></td>
</tr>
<tr>
<td><strong>6. 上传</strong></td>
<td><code>hexo d</code></td>
<td>推送到 GitHub 服务器</td>
</tr>
</tbody></table>
<blockquote>
<p><strong>⚠️ 常见犯错提示：</strong></p>
<ul>
<li><p><strong>网络连接重置</strong>：报错 <code>Connection was reset</code> 或 <code>Failed to connect</code> 时，检查你的 Git 代理设置。如果你没开代理软件，运行 <code>git config --global --unset http.proxy</code> 取消代理。</p>
</li>
<li><p><strong>图片无法显示</strong>：笔记中的图片路径如果指向你电脑本地（如 <code>C:\Users\...</code>），上传后博客会“碎图”。建议学习使用 <strong>PicGo 图床</strong> 或将图片放入 <code>source/images</code> 文件夹并使用相对路径。</p>
</li>
<li><p><strong>源码丢失</strong>：<code>hexo d</code> 只会上传“网页结果”，<strong>不会备份你的 .md 原稿</strong>。建议将整个博客文件夹再建立一个私有仓库备份，防止电脑宕机。</p>
</li>
</ul>
</blockquote>
<h2 id="“双仓库法”"><a href="#“双仓库法”" class="headerlink" title="“双仓库法”"></a>“<strong>双仓库法</strong>”</h2><p>：一个公开仓库展示博客，一个私有仓库保存源码。</p>
<hr>
<h3 id="第一步：在-GitHub-上创建一个私有仓库"><a href="#第一步：在-GitHub-上创建一个私有仓库" class="headerlink" title="第一步：在 GitHub 上创建一个私有仓库"></a>第一步：在 GitHub 上创建一个私有仓库</h3><ol>
<li><p>登录 GitHub，点击右上角的 <strong>+</strong> -&gt; <strong>New repository</strong>。</p>
</li>
<li><p><strong>Repository name</strong>：建议起名为 <code>hexo-source</code> 或 <code>blog-backup</code>。</p>
</li>
<li><p><strong>Public&#x2F;Private</strong>：务必选择 <strong>Private</strong>（私有），这样别人就看不到你的源码和未发布的草稿。</p>
</li>
<li><p><strong>不要</strong>勾选 “Initialize this repository with a README”。</p>
</li>
<li><p>点击 <strong>Create repository</strong>，复制生成的仓库地址（如 <code>https://github.com/用户名/hexo-source.git</code>）。</p>
</li>
</ol>
<h3 id="第二步：配置本地-gitignore（关键防错）"><a href="#第二步：配置本地-gitignore（关键防错）" class="headerlink" title="第二步：配置本地 .gitignore（关键防错）"></a>第二步：配置本地 <code>.gitignore</code>（关键防错）</h3><p>在你的本地博客根目录（<code>my_blog</code>）下，检查是否存在 <code>.gitignore</code> 文件。如果没有，新建一个。这个文件的作用是告诉 Git <strong>哪些文件不要备份</strong>（比如几百 MB 的依赖包）。</p>
<p>请确保 <code>.gitignore</code> 中包含以下内容：</p>
<p>Plaintext</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.DS_Store</span><br><span class="line">Thumbs.db</span><br><span class="line">db.json</span><br><span class="line">*.log</span><br><span class="line">node_modules/</span><br><span class="line">public/</span><br><span class="line">.deploy_git/</span><br></pre></td></tr></table></figure>

<h3 id="第三步：初始化-Git-并推送到私有仓库"><a href="#第三步：初始化-Git-并推送到私有仓库" class="headerlink" title="第三步：初始化 Git 并推送到私有仓库"></a>第三步：初始化 Git 并推送到私有仓库</h3><p>在你的本地 <code>my_blog</code> 文件夹下打开终端，依次执行以下命令：</p>
<ol>
<li><p><strong>初始化 Git</strong>：</p>
<p> Bash</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git init</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>关联私有仓库</strong>：</p>
<p> Bash</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git remote add origin https://github.com/你的用户名/你的私有仓库名.git</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>提交并上传</strong>：</p>
<p> Bash</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git add .</span><br><span class="line">git commit -m &quot;Backup hexo source code&quot;</span><br><span class="line">git push -u origin master (或 main)</span><br></pre></td></tr></table></figure></li>
</ol>
<h2 id="Butterfly-主题"><a href="#Butterfly-主题" class="headerlink" title="Butterfly 主题"></a>Butterfly 主题</h2><h3 id="第一步：安装主题及核心依赖"><a href="#第一步：安装主题及核心依赖" class="headerlink" title="第一步：安装主题及核心依赖"></a>第一步：安装主题及核心依赖</h3><p>Butterfly 强依赖于 <strong>Pug</strong> 和 <strong>Stylus</strong> 渲染引擎，所以你必须安装它们才能正常显示网页。</p>
<ol>
<li><p><strong>在博客根目录打开终端</strong>，运行安装命令：</p>
<p> Bash</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install hexo-theme-butterfly --save</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>安装必要的渲染插件</strong>（如果不装，网页会显示乱码或报错）：</p>
<p> Bash</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install hexo-renderer-pug hexo-renderer-stylus --save</span><br></pre></td></tr></table></figure></li>
</ol>
<blockquote>
<p><strong>⚠️ 犯错预警（网络连接）：</strong> 你之前遇到了 <code>port 443</code> 连接超时的问题。如果 <code>npm install</code> 报错，请务必开启你的代理工具，或切换为国内镜像源： <code>npm config set registry https://registry.npmmirror.com</code></p>
</blockquote>
<hr>
<h3 id="第二步：应用主题"><a href="#第二步：应用主题" class="headerlink" title="第二步：应用主题"></a>第二步：应用主题</h3><ol>
<li><p>打开博客根目录下的 <strong>主配置文件</strong> <code>_config.yml</code>。</p>
</li>
<li><p>找到 <code>theme</code> 字段，修改为：</p>
<p> YAML</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">theme: butterfly</span><br></pre></td></tr></table></figure></li>
</ol>
<hr>
<h3 id="第三步：创建并编辑主题配置文件"><a href="#第三步：创建并编辑主题配置文件" class="headerlink" title="第三步：创建并编辑主题配置文件"></a>第三步：创建并编辑主题配置文件</h3><p>为了方便以后升级主题而不丢失配置，建议<strong>不要</strong>直接去 <code>node_modules</code> 里改文件。</p>
<ol>
<li><p>在博客根目录下新建一个文件，命名为：<strong><code>_config.butterfly.yml</code></strong>。</p>
</li>
<li><p>你可以从 <a href="https://butterfly.js.org/posts/21cfbf15/">Butterfly 官网配置文档</a> 复制默认配置进去，或者先保持空白，逐步添加你需要的个性化设置（如头像、菜单等）。</p>
</li>
</ol>
<hr>
<h3 id="第四步：本地预览与清理"><a href="#第四步：本地预览与清理" class="headerlink" title="第四步：本地预览与清理"></a>第四步：本地预览与清理</h3><p>每当更换主题或重大配置，必须执行“清理三部曲”：</p>
<p>Bash</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo clean</span><br><span class="line">hexo g</span><br><span class="line">hexo s</span><br></pre></td></tr></table></figure>

<ul>
<li>访问 <code>http://localhost:4000</code> 看看你的新主页是否变成了 Butterfly 风格。</li>
</ul>
]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>Github</tag>
      </tags>
  </entry>
  <entry>
    <title>github上传项目</title>
    <url>/2026/01/07/%E4%B8%8A%E4%BC%A0%E9%A1%B9%E7%9B%AE/</url>
    <content><![CDATA[<h3 id="第一步：在-GitHub-上创建新仓库"><a href="#第一步：在-GitHub-上创建新仓库" class="headerlink" title="第一步：在 GitHub 上创建新仓库"></a>第一步：在 GitHub 上创建新仓库</h3><ol>
<li><p>登录 GitHub。</p>
</li>
<li><p>点击右上角的 <strong>+</strong> 号 -&gt; <strong>New repository</strong>。</p>
</li>
<li><p>输入仓库名称（例如 <code>workflow-gateway</code>）。</p>
</li>
<li><p><strong>不要</strong>勾选 “Initialize this repository with a README”（保持仓库为空）。</p>
</li>
<li><p>点击 <strong>Create repository</strong>。</p>
</li>
<li><p>复制屏幕上显示的 HTTPS 或 SSH 地址（例如 <code>https://github.com/你的用户名/workflow-gateway.git</code>）。</p>
</li>
</ol>
<h3 id="第二步：清理并初始化本地仓库"><a href="#第二步：清理并初始化本地仓库" class="headerlink" title="第二步：清理并初始化本地仓库"></a>第二步：清理并初始化本地仓库</h3><p>打开终端，进入你的源代码根目录（也就是包含 <code>CMakeLists.txt</code> 的那一层）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 进入项目目录 (根据你的实际路径调整)</span></span><br><span class="line"><span class="built_in">cd</span> ~/cpp58/gateway/gateway</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 初始化 Git 仓库</span></span><br><span class="line">git init</span><br></pre></td></tr></table></figure>

<h3 id="第三步：创建-gitignore-文件-关键"><a href="#第三步：创建-gitignore-文件-关键" class="headerlink" title="第三步：创建 .gitignore 文件 (关键!)"></a>第三步：创建 <code>.gitignore</code> 文件 (关键!)</h3><p>我们需要告诉 Git 忽略掉 <code>build</code> 文件夹和编译出来的二进制文件。 在终端中执行以下命令创建忽略规则：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建 .gitignore 文件</span></span><br><span class="line"><span class="built_in">cat</span> &lt;&lt;<span class="string">EOF &gt; .gitignore</span></span><br><span class="line"><span class="string"># 忽略编译生成的目录</span></span><br><span class="line"><span class="string">build/</span></span><br><span class="line"><span class="string">cmake-build-debug/</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 忽略可执行文件</span></span><br><span class="line"><span class="string">gateway</span></span><br><span class="line"><span class="string">server/server</span></span><br><span class="line"><span class="string">test/benchmark</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 忽略临时对象文件</span></span><br><span class="line"><span class="string">*.o</span></span><br><span class="line"><span class="string">*.d</span></span><br><span class="line"><span class="string">*.a</span></span><br><span class="line"><span class="string">*.so</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 忽略 IDE 配置 (可选)</span></span><br><span class="line"><span class="string">.vscode/</span></span><br><span class="line"><span class="string">.idea/</span></span><br><span class="line"><span class="string">.DS_Store</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure>

<h3 id="第四步：提交代码到本地"><a href="#第四步：提交代码到本地" class="headerlink" title="第四步：提交代码到本地"></a>第四步：提交代码到本地</h3><p>现在将剩下的源代码文件添加到暂存区并提交：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 添加当前目录下所有文件 (Git 会自动遵守 .gitignore 的规则)</span></span><br><span class="line">git add .</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 检查一下状态，确保 build 目录没有被添加进去</span></span><br><span class="line">git status</span><br><span class="line"><span class="comment"># (你应该看到 new file: src/..., config/... 等，但不应该看到 build/...)</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 提交</span></span><br><span class="line">git commit -m <span class="string">&quot;Initial commit: Workflow Gateway v1.0&quot;</span></span><br></pre></td></tr></table></figure>

<h3 id="第五步：推送到-GitHub"><a href="#第五步：推送到-GitHub" class="headerlink" title="第五步：推送到 GitHub"></a>第五步：推送到 GitHub</h3><p>将本地仓库与刚才在 GitHub 上创建的远程仓库关联，并推送代码：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 将分支重命名为 main (现在的标准做法)</span></span><br><span class="line">git branch -M main</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 关联远程仓库 (替换为你自己的 GitHub 地址)</span></span><br><span class="line">git remote add origin https://github.com/你的用户名/workflow-gateway.git</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 推送代码</span></span><br><span class="line">git push -u origin main</span><br></pre></td></tr></table></figure>

<h3 id="常见问题解决"><a href="#常见问题解决" class="headerlink" title="常见问题解决"></a>常见问题解决</h3><ol>
<li><p><strong>如果你之前已经不小心把 <code>build</code> 提交了</strong>： 执行以下命令将其从 Git 记录中删除（但不删除本地文件）：</p>
<p> Bash</p>
 <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">rm</span> -r --cached build</span><br><span class="line">git commit -m <span class="string">&quot;Stop tracking build directory&quot;</span></span><br><span class="line">git push</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>密码验证问题</strong>： 自 2021 年起，GitHub 命令行不再支持密码验证。如果你在 <code>git push</code> 时被询问密码：</p>
<ul>
<li><p><strong>用户名</strong>：输入你的 GitHub 用户名。</p>
</li>
<li><p><strong>密码</strong>：必须输入 <strong>Personal Access Token (PAT)</strong>。</p>
<ul>
<li>去 GitHub 设置 -&gt; Developer settings -&gt; Personal access tokens -&gt; Tokens (classic) -&gt; Generate new token -&gt; 勾选 <code>repo</code> 权限 -&gt; 生成并复制那个以 <code>ghp_</code> 开头的字符串作为密码。</li>
</ul>
</li>
<li><p>或者，如果你配置了 SSH Key，建议使用 SSH 地址 (<code>git@github.com:...</code>) 代替 HTTPS 地址。</p>
</li>
</ul>
</li>
<li><p><strong>重新生成 Token (一定要选 Classic)</strong></p>
<ul>
<li>登录 GitHub，点击右上角头像 -&gt; <strong>Settings</strong>。</li>
</ul>
<ul>
<li><p>在左侧最底部找到 <strong>Developer settings</strong>。</p>
</li>
<li><p>点击 <strong>Personal access tokens</strong> -&gt; 选择 <strong>Tokens (classic)</strong> (不要选 Fine-grained)。</p>
</li>
<li><p>点击 <strong>Generate new token</strong> -&gt; <strong>Generate new token (classic)</strong>。</p>
</li>
<li><p><strong>【关键步骤】</strong>：</p>
<ul>
<li><p><strong>Note</strong>: 随便填（比如 <code>gateway-push</code>）。</p>
</li>
<li><p><strong>Expiration</strong>: 建议选 <code>No expiration</code>（永不过期，或者是 30 天）。</p>
</li>
<li><p><strong>Select scopes</strong>: <strong>一定要勾选第一个 <code>repo</code> 复选框</strong>（这包含了对仓库的所有读写权限）。</p>
</li>
</ul>
</li>
<li><p>点击底部的 <strong>Generate token</strong>。</p>
</li>
<li><p><strong>复制</strong>那个以 <code>ghp_</code> 开头的字符串。</p>
</li>
</ul>
</li>
</ol>
]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>Github</tag>
      </tags>
  </entry>
</search>
